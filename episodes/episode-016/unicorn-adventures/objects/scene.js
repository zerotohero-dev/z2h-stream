/*
 *  \
 *  \\,
 *   \\\,^,.,,.                    “Zero to Hero”
 *   ,;7~((\))`;;,,               <zerotohero.dev>
 *   ,(@') ;)`))\;;',    stay up to date, be curious: learn
 *    )  . ),((  ))\;,
 *   /;`,,/7),)) )) )\,,
 *  (& )`   (,((,((;( ))\,
 */

const bg = new Image();
bg.src = '/background.png';
const bgWidth = 1920;
const bgHeight = 1080;

const scene = {
  image: bg,
  height: bgHeight,
  width: bgWidth
};

export default scene;
