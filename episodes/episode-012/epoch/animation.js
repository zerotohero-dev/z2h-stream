/*
 *  \
 *  \\,
 *   \\\,^,.,,.                    “Zero to Hero”
 *   ,;7~((\))`;;,,               <zerotohero.dev>
 *   ,(@') ;)`))\;;',    stay up to date, be curious: learn
 *    )  . ),((  ))\;,
 *   /;`,,/7),)) )) )\,,
 *  (& )`   (,((,((;( ))\,
 */

import {clsf, show} from "./dom";

export const showAnimation = () => {
  const popper = clsf('party-popper');
  const cannon = clsf('cannon');
  show(popper);
  show(cannon);
};
