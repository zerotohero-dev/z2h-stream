/*
 *  \
 *  \\,
 *   \\\,^,.,,.                    “Zero to Hero”
 *   ,;7~((\))`;;,,               <zerotohero.dev>
 *   ,(@') ;)`))\;;',    stay up to date, be curious: learn
 *    )  . ),((  ))\;,
 *   /;`,,/7),)) )) )\,,
 *  (& )`   (,((,((;( ))\,
 */

export const cls = (c) => document.getElementsByClassName(c);
export const tag = (t) => document.getElementsByTagName(t);
export const clsf = (s) => cls(s)[0];
export const tagf = (s) => tag(s)[0];
export const show = (el) => el.style.display = '';
